================================================================================
  OKX 量化交易机器人 · 可调参数说明书
================================================================================

【0】怎么改参数
--------------------------------------------------------------------------------

改参数的三个入口（三选一，都不用重新打包 exe）：

  ① 启动后按 s        → 进入「参数调节」菜单，按分组逐项改，改完能立刻看到生效值
  ② 外置配置文件       → 在 exe 同目录放 regime_config.json，写 "USER" 段：
                          { "USER": { "max_positions": 15, "stop_atr": 2.5 } }
                        （EXE 内嵌了一份默认配置，外置文件里的项会覆盖内嵌值）
  ③ 命令行             → OKXBot_v35.exe --set USER.max_positions=15
                        多个用空格隔开；--set 不写前缀默认进 SNAPSHOT 段

查看当前生效值：启动后按 s 看菜单，或 OKXBot_v35.exe --print-config

★ 三条内置保护（你绕不过去，这是刻意设计的）
  · 白名单制：不在参数表里的键一律忽略并告警（防拼错一个字母「以为改了其实没改」）
  · 槽位守卫：持仓上限会自动被抬到「能把钱投出去」所需的最小值
  · 占比守卫：单仓占比小到需要超过 100 个仓时，直接拒绝该值


【1】先说两条不可调的底线（为什么它们不让改）
--------------------------------------------------------------------------------
这两个不是「参数」，是算法的目标本身，任何参数都不能破坏它们：

  底线① 总敞口 w = 已投资金额 ÷ 账户权益 ≤ E
         （E 就是下面的 exposure_target，默认 0.985 = 用满）
  底线② 每轮尽量把预算投满（per_max 自动托底 + 余量循环补投）

原因：这两个值直接决定「投多少钱」，属于 USDT 最大化原则本身。
所以 CAPITAL_UTILIZATION / KELLY_FRACTION / per_max / min_trade_usdt /
leverage / td_mode 这一类键被【明令禁止】写在 USER 段（写了会被告警拒绝）。

「可调参数」只允许影响三件事：选币、择时、退出细节。
这样你可以随便调，都不会调出「钱投不出去」的结果。


【2】参数总表
--------------------------------------------------------------------------------
格式：名称 | 类型 | 范围 | 默认 | 一句话

▌2.1 敞口（投多少钱出去 —— 唯一能间接影响 E 的参数）
--------------------------------------------------------------------------------
exposure_target | float | 0 ~ 0.985 | 0.985 | 目标总敞口 E，占权益比例

  是什么：账户权益的多少比例允许同时压在仓位上。0.985 = 几乎用满。
  为什么默认 0.985：留 1.5% 缓冲应对「下单瞬间价格波动导致略超额度」，
    以及手续费扣减。设成 1.000 会因为浮点/滑点导致最后一笔下不出去。
  调低的后果（具体场景）：设 0.5 时，账户 100U 只会用 50U 买币，剩 50U 一直闲着。
  公式：    已投资金额 ÷ 权益 ≤ exposure_target

▌2.2 分散化（开几个仓、每个仓多大）
--------------------------------------------------------------------------------
max_positions        | int   | 1 ~ 100 | 10   | 同时持仓上限
max_position_ratio   | float | 0.05 ~ 1.00 | 0.25 | 单仓上限，占权益比例
pick_n               | int   | 1 ~ 30  | 10   | 激进型目标清单长度
target_pool          | int   | 1 ~ 50  | 10   | 两阶段选币的目标池大小
backup_n             | int   | 0 ~ 50  | 10   | 备选递补队列长度
thin_slot_guard      | bool  | —       | 开   | 单仓摊薄保护

  ● max_positions 是什么：最多同时持几个币。默认 10。
    为什么默认 10：等权 n 仓的组合方差 D(n) = ρ̄ + (1−ρ̄)/n（ρ̄=平均相关性）。
      n 越大方差越小，但收益递减——再加一个仓只把方差压下 (1−ρ̄)/(n(n+1))。
      加密币平均相关性 ρ̄≈0.7~0.8，n 到 10 以后多开的仓「只是白付手续费和监控成本」。
    ★★ 重要：实际槽位是【双重约束】，取两者的小值：
        实际槽位 = min(pick_n, max_positions)
      所以你只调 max_positions 而 pick_n 还停在 10 时，调到 20 也没用（会被卡在 10）。
      程序已处理：你没显式写 pick_n 时会自动同步抬到同一水位。
    自动抬高（槽位守卫）：如果 10 个仓装不下你的总敞口，会自动抬到
        需要槽位 = max(1, ⌈ E ÷ 单仓占比 ⌉)
      例：E=0.985、单仓占比 0.05 → ⌈0.985/0.05⌉ = 20 → 持仓上限自动抬到 20。
      上不封顶到 100（硬上限 SLOTS_HARD_CAP=100）。

  ● max_position_ratio 是什么：单个币最多占权益的多少（防止一个币爆掉拖垮全账户）。
    默认 0.25 = 单币最多 25% 权益。范围 0.05~1.00。
    调小的后果：0.05 → 要开 20 个仓才能把 98.5% 的钱投出去；
      如果单仓金额小到低于交易所最小成交额（minSz × 现价），这钱就投不出去了。
    占比守卫：⌈E ÷ max_position_ratio⌉ > 100 时直接拒绝该值。

  ● pick_n / target_pool / backup_n：选币链路的三个「队列长度」。
    pick_n       = 激进型最终要挑几个币（买前几名）
    target_pool  = 两阶段选币的目标池大小（先粗筛到这个规模再精筛）
    backup_n     = 备选递补队列（首选币买不进时按顺序递补）
    默认都是 10；调大可以增加覆盖，但每轮扫描耗时也会增加。

  ● thin_slot_guard（单仓摊薄保护，默认开）：
    是什么：钱太少时自动把槽位【收窄】，保证每一笔都下得出去。
    为什么需要：100U 分 10 个仓 = 每仓 10U；但 1.6U 分 10 个仓 = 每仓 0.16U，
      而交易所最小成交额常常要 ~0.39U → 每仓都下不出去，钱全闲置
      （日志里会出现 `IDLE x.xxU 闲置`）。
    公式：单笔额度 = 可用预算 ÷ 槽位；若 单笔额度 < (minSz × 现价) → 收窄槽位，
      直到每一笔都下得出去为止。只收窄、不收宽。
    关掉 = 完全按 max_positions 开，宁可少买也不减槽位。
    ★ 提醒：这个守卫必须排在 per_max 托底【之前】计算——因为托底额是按槽位算的
      (_auto_cap = 预算 ÷ 槽位)，先托底再收窄会让数值卡在旧的小值上。

▌2.3 选币（买什么样的币、多久重算一次）
--------------------------------------------------------------------------------
min_cand_score   | float | 0 ~ 100     | 55    | 选币最低综合评分（0=不设门槛）
min_vol_usdt     | float | 0 ~ 1e9     | 500000| 24h 成交额下限（U）
scan_mult        | int   | 1 ~ 20      | 4     | 扫描名额倍数
scan_min         | int   | 1 ~ 400     | 40    | 扫描名额下限
liq_share        | float | 0 ~ 1.0     | 0.6   | 流动性榜占扫描名额比例
max_amp_pct      | float | 5 ~ 500     | 45    | 24h 振幅上限%（妖币剔除）
breadth_gate     | float | 0 ~ 3.0     | 1.2   | 弱势市场门槛上浮倍数（1.0=关闭）
refresh_rounds   | int   | 1 ~ 100     | 3     | 候选池刷新间隔（轮）
cooldown_rounds  | int   | 0 ~ 100     | 3     | 买入失败后冷却轮数

  ● min_cand_score：综合评分低于这个数就不买。默认 55（满分 100）。
    公式（真实生效门槛）：
        门槛 = min_cand_score × (市场宽度 < 25% ? breadth_gate : 1)
        市场宽度 = 上涨币数 ÷ USDT 交易对总数
    例：宽度 8.4%（普跌）→ 门槛 55 × 1.2 = 66 → 只买最强的。
    ★ 为什么需要这条：策略只做上涨币（ch>0）。普跌行情下这条规则会把 BTC/ETH
      等优质币【全部剔除】，剩下的必然是「大盘跌 3% 它还能涨」的逆势拉升
      小币/妖币（多半是拉高出货）→ 系统在弱市里被迫专挑最危险的币买。
      实测 2026-09-24：406 个 USDT 对只有 34 个上涨（宽度 8.4%），当天买的
      TRUMP 收盘 -10.4%。抬高门槛后，弱市只买最强的，而不是硬停摆
      （停摆 = 钱闲置，违背 USDT 最大化）。
    设 breadth_gate = 1.0 就是关闭这个功能（回到「不看市场状态」）。

  ● min_vol_usdt：24h 成交额低于这个数的币直接不看。默认 50 万 U。
    为什么：成交额太小的币买得进、卖不出（滑点吃掉利润），且容易被操纵。

  ● scan_mult / scan_min（扫描名额）：
    公式：扫描名额 = max(target_pool × scan_mult, scan_min)
    默认 target_pool=10、mult=4 → 40 个名额。名额越大，越不容易漏掉好币，
    但每轮要拉的 K 线也越多（更慢、更耗接口额度）。

  ● liq_share：扫描名额怎么分配（v34 新增「双榜取样」）。
    公式：流动性榜取 liq_share 比例，涨幅榜取剩下的 (1 - liq_share)。
    默认 0.6 = 六成名额给「成交额最大的币」，四成给「涨幅最高的币」。
    为什么两个榜都要：只看涨幅会选到小市值妖币；只看成交额会总是 BTC/ETH。
    设 0 = 纯涨幅榜（旧行为），设 1 = 纯流动性榜。
    ★ 注意：这个改动【池子大时才见效】。实测池子只有 9 个时，双榜与单榜
      结果完全重合（9/9）。

  ● max_amp_pct：24h 振幅上限，超过就当成妖币剔除。默认 45%。
    公式：振幅% = (最高价 − 最低价) ÷ 最新价 × 100
    另有下限 3%（死水剔除，内部常量 AGGRESSIVE_MIN_AMP_PCT=3.0）：
      振幅太小说明没波动，赚不到钱。
    ★ 数据缺失时【放行】（缺数据 ≠ 异常，不因缺字段剔人）。

  ● refresh_rounds：候选池每多少轮重算一次。默认 3 轮。
    调小 = 反应更快但更耗接口；调大 = 更省接口但可能拿着过期的榜单。

  ● cooldown_rounds：某个币买入失败后，多少轮内不再碰它。默认 3 轮。
    为什么：刚失败的币往往还在「异常状态」，立刻重试大概率继续失败。

  ● 附带说明（内部固定、不开放）：稳定币过滤 STABLE_BASES 会把 USDC/TUSD/
    FDUSD 等 12 个稳定币排除在选币之外——它们涨幅≈0 也能「通过上涨判定」，
    白占名额（实测曾排到成交额第 3 名）。

▌2.4 相关性（两个库里的币太像怎么办）
--------------------------------------------------------------------------------
corr_free | float | 0 ~ 1.0 | 0.80 | 相关性低于此值不做折减
corr_hard | float | 0 ~ 1.0 | 0.98 | 相关性高于此值视为冗余、跳过

  是什么：衡量「两个币的走势有多像」（1.0 = 完全同步）。
  为什么要有：买 5 个走势完全一样的币，等于把 5 份钱押在一个方向上——
    风险没有分散，只是手续费翻了 5 倍。
  用法：相关性 < corr_free(0.80) 视为「够独立」，正常买；
        相关性 > corr_hard(0.98) 视为「几乎同一个币」，跳过。

▌2.5 退出（什么时候卖 —— 参数最多的一组）
--------------------------------------------------------------------------------
stop_atr           | float | 0.5 ~ 10.0  | 3.0     | 硬止损宽度（ATR 倍数）
trail_dd_target    | float | 0.002 ~ 0.30| 0.025   | 吊灯回撤目标（占价格比例）
peak_lock_arm_atr  | float | 0 ~ 10.0    | 1.0     | 峰值锁定启用阈值（ATR 倍数）
peak_lock_keep     | float | 0 ~ 1.0     | 0.65    | 峰值锁定保留比例
trail_tight_n      | float | 0.1 ~ 4.0   | 0.6     | 盈利后吊灯带宽下限（n×ATR）
top_signals        | int   | 0 ~ 3       | 2       | 顶部共振信号数
bayes_theta        | float | -0.02 ~ 0.0 | -0.0015 | 贝叶斯机会成本阈值（每小时收益）
time_exit_days     | float | 0.25 ~ 60   | 1.0     | 时间止损（天）
trend_reversal_t   | float | -6.0 ~ 0.0  | -2.0    | 趋势反转 t 阈值
vol_spike_mult     | float | 1.1 ~ 10.0  | 2.5     | 波动突变倍数
max_hold_days      | float | 0.5 ~ 120   | 3.0     | 最长持有（天），超时清仓
min_profit_ratio   | float | -0.10 ~ 0.50| 0.003   | 「有意义的盈利」门槛

  卖出是分层的，按顺序判断，任一层触发就卖：
    L1 硬止损 → L2 保本 → L2.5 峰值锁定棘轮 → L3 自适应ATR吊灯 →
    L3.5 趋势反转 → L4 贝叶斯 → L5 时间止损 → L6 效率比 → L7 波动突变 →
    L7.5 顶部共振 → L9 EMA20破位/超时

  ── L1 stop_atr（硬止损）──
    公式：止损价 = 成本价 − stop_atr × ATR
    为什么默认 3.0：按实际盈亏分布反推。统计「无止损持有 72 小时」的样本中，
      最终盈利的单子，其最大浮亏（MAE）90 分位 = 5.81%。
      → 止损必须【宽于 5.81%】才不会误杀本来会涨回来的单子。
      取 3.0 ATR（ATR≈2% 时 ≈ 6%），刚好略高于该分位。
    ★ 它只负责「黑天鹅保护」，不负责「及时」——及时由 L3 贝叶斯 + L5 时间止损
      负责，那两层远早于 L1 触发。
    ★★ 隐藏陷阱：`trail_dd_target` 看起来是「回撤 2.5% 就卖」，其实不是。
      吊灯带宽公式是：
          n = clamp( trail_dd_target ÷ ATR% , n下限 , 4.0 )
          止损价 = 最高价 − n × ATR
      ATR 是「平均真实波幅%」。当 ATR=6%（高波动币）时：
          0.025 ÷ 0.06 = 0.42 → 被下限钳到 1.0
          → 实际带宽 = 1.0 × ATR = 6%
      也就是说：调 trail_dd_target 在 ATR 大的币上【几乎没有效果】，
      要看的是「钳位之后的 n」。想真正收窄，要动 n 的下限（见 trail_tight_n）。

  ── L2.5 peak_lock_arm_atr / peak_lock_keep（峰值锁定棘轮）★★ 最值得理解的一层 ──
    解决什么问题：原来自动止盈只有「吊灯」一种，带宽固定 = n×ATR（默认 1.0×ATR
      ≈ 3%），而 60 秒轮询下遇到「一根 K 线里拉高又砸回」，吊灯要等回落满 1ATR
      才响——实测经常在 +1% 附近才成交（峰值可能已经 +8%）。
      你看到的「没在最高点卖，反而卖在很低」，就是这个原因。
    棘轮的做法：把「已经拿到过的最高浮盈」变成一个【只升不降的地板】。
      启用条件：峰值浮盈 ≥ peak_lock_arm_atr × ATR
      地板公式：地板价 = 成本价 × (1 + peak_lock_keep × 峰值盈利率)
      例：峰值 +10%、keep=0.65 → 地板 = +6.5%，最坏也只回吐 35% 的涨幅。
    为什么 arm 取 1.0×ATR 而不是更保守的 1.5×ATR：
      实测门槛 1.5 时，「高波动 + 小幅上涨」这一最常见的小微币形态
      （ATR 6%、峰值仅 +8%）完全不受益——因为 1.5×6% = 9% > 8%，
      棘轮压根没启动，回吐仍是 6.02%。
      降到 1.0×ATR 后该场景卖价 101.50 → 105.00，回吐收到 2.78%。
    keep=0 直接关闭这一层（退回旧版行为）。

  ── L3 trail_tight_n（盈利单的吊灯收紧）──
    公式：峰值浮盈 ≥ 1.0×ATR 后，吊灯的 n 下限从 1.0 降到 trail_tight_n(0.6)：
          回撤带由 ~3% 收到 ~1.8%；高波动币从 ~6% 收到 ~3.6%。
    默认 0.6。越小 = 越贴近最高点卖出，但越容易被正常波动扫出去（卖早了）。

  ── L7.5 top_signals（顶部共振）──
    公式：同时命中下列信号 ≥ top_signals 个就卖（默认 2 个）
          · RSI   ≥ 78
          · 布林带位置 BB% ≥ 0.97（贴着上轨）
          · KDJ_J ≥ 105（超买）
    为什么：与其等价格跌下来被动止盈，不如在「强势还没转弱」时主动兑现。
    设 0 = 关闭这一层。

  ── L4 bayes_theta ──
    公式：预期每小时收益 < bayes_theta 时卖出（机会成本比较）。
    默认 -0.0015（即每小时 -0.15%）。调得更负（如 -0.005）= 更迟钝、更能拿得住。

  ── L3.5 trend_reversal_t ──
    是什么：用回归斜率的 t 检验确认「趋势真的反转了」。
    默认 -2.0。★ 越负越迟钝（-6 表示要非常显著的反转才卖）。

  ── L5 time_exit_days / L9 max_hold_days（时间维度）──
    time_exit_days 默认 1 天：拿满 1 天还没起色就走。
    max_hold_days  默认 3 天：硬上限，到点无条件清仓。
    ★ 内部统一按「小时」计算，写入时 ×24；旧写法 *_hours 会自动 ÷24 折算
      （不会静默失效，也会正常提示）。

  ── L7 vol_spike_mult ──
    公式：当前波动 > vol_spike_mult × 平均波动 → 视为异动，退出。
    默认 2.5。调小 = 更敏感（更容易被洗出来）。

  ── min_profit_ratio ──
    是什么：「算得上有盈利」的门槛，用于顶部共振 / 保本判断等前置条件。
    默认 0.003（+0.3%）。设太大会导致小赚的单子不被保护。

▌2.6 熔断（整个组合的保护闸）
--------------------------------------------------------------------------------
portfolio_max_dd    | float | 0.01 ~ 0.90 | 0.12 | 峰值回撤熔断线 → 全平 + 冷却
portfolio_daily_dd  | float | 0.005 ~ 0.5 | 0.04 | 日内回撤熔断线 → 暂停开新仓
portfolio_cooldown_h| float | 0.5 ~ 240   | 24.0 | 熔断后冷却小时数

  是什么：不看单个币，看【整个账户】。
  ● portfolio_max_dd：账户从最高点回撤到 12% → 全部平仓 + 进入冷却。
  ● portfolio_daily_dd：当天回撤 4% → 停止开新仓（已有仓位不动）。
  ● portfolio_cooldown_h：熔断后冷却 24 小时，期间不开新仓。
  为什么要有：单币止损管不了「所有币一起跌」的情况（加密市场相关性高，
    普跌时几乎同涨同跌）。熔断是账户级的最后一道闸。

▌2.7 归集（把零散币换成 USDT）
--------------------------------------------------------------------------------
collect_min_usdt | float | 0 ~ 100 | 0.5 | 归集门槛：零散币市值低于此值不卖（U）
dust_value_usdt  | float | 0 ~ 100 | 0.1 | 尘埃门槛：剩余市值低于此值即不再算持仓（U）

  ● collect_min_usdt：市值低于 0.5U 的零散币不去卖（省手续费，避免白忙）。
    ★★ 重要：归集实际有【两道门槛】，都要过才卖：
        ① 市值 ≥ collect_min_usdt
        ② 数量 ≥ 交易所最小下单量 minSz
      这是 v34 修的一个真 bug：原来只判市值不看数量，实盘 OKB 余额
      0.000902022 个（最小要 0.01 个）、市值 0.1127U 恰好越过 0.11U 门槛
      → 每轮归集都去下单、每轮必失败，刷出 431 条 COLLECT-FAIL。
    ★ 通用教训：凡「按金额判门槛」的地方，都要同时问一句「数量下得出去吗」。
  ● dust_value_usdt：某个币卖剩一点点（< 0.1U）时，不再算作持仓（避免一直显示）。


【3】公式汇总（速查）
--------------------------------------------------------------------------------
总敞口上限        : 已投资金额 ÷ 权益 ≤ exposure_target
实际槽位          : min(pick_n, max_positions)
槽位守卫(自动抬高): 需要槽位 = max(1, ⌈E ÷ max_position_ratio⌉)
占比守卫(拒绝)    : ⌈E ÷ max_position_ratio⌉ > 100 → 拒绝该值
摊薄保护          : 单笔额度 = 预算 ÷ 槽位 ≥ (minSz × 现价)
最小下单金额      : minSz × 现价
硬止损            : 止损价 = 成本 − stop_atr × ATR
吊灯带宽          : n = clamp(trail_dd_target ÷ ATR%, n下限, 4.0)
吊灯止损          : 止损价 = 最高价 − n × ATR
峰值锁定地板      : 地板 = 成本 × (1 + peak_lock_keep × 峰值盈利率)
                    (启用条件: 峰值浮盈 ≥ peak_lock_arm_atr × ATR)
顶部共振          : 命中(RSI≥78 / BB%≥0.97 / KDJ_J≥105) ≥ top_signals → 卖
入场门槛          : min_cand_score × (市场宽度 < 25% ? breadth_gate : 1)
市场宽度          : 上涨币数 ÷ USDT 交易对总数
综合评分(0~100)   : 动量×0.20 + RSI×0.18 + ADX×0.18 + 成交量×0.13
                    + 布林×0.10 + 波动率×0.09 + SuperTrend×0.12   (权重和=1.00)
扫描名额          : max(target_pool × scan_mult, scan_min)
双榜取样          : 流动性榜占 liq_share，涨幅榜占 (1 − liq_share)
振幅              : (最高价 − 最低价) ÷ 最新价 × 100
归集可卖          : 市值 ≥ collect_min_usdt 且 数量 ≥ minSz
尘埃              : 剩余市值 < dust_value_usdt → 不再算持仓


【4】只读的内部常量（为什么不让改）
--------------------------------------------------------------------------------
这些是算法的「物理常数」，写死在代码里，改了会破坏上面那些公式的前提：

  DIVERSIFY_SLOTS_MIN = 10    槽位下限（引擎按相关性算出的建议值不会低于它）
  SLOTS_HARD_CAP      = 100   槽位硬上限
  TRAIL_N_MIN         = 1.0   吊灯带宽绝对下限
  TRAIL_N_MAX         = 4.0   吊灯带宽绝对上限
  TRAIL_TIGHT_ARM_ATR = 1.0   盈利单收紧的启用门槛
  AGGR_BREADTH_WEAK   = 0.25  判定「弱市」的宽度阈值（25%）
  AGGR_BTC_CRASH_PCT  = 5.0   BTC 24h 跌 5% → 本轮不开新仓（黑天鹅保护）
  AGGRESSIVE_MIN_AMP_PCT = 3.0 振幅下限（死水剔除）
  STABLE_BASES              12 个稳定币（USDC/TUSD/FDUSD...），选币时排除
  STOP_ATR_END / STOP_DECAY_HOURS  止损随时间衰减（默认不衰减）
  BREAK_EVEN_ATR_MULT = 0.8  L2 保本的触发倍数
  MIN_TRADE_USDT             程序自设的最小买入额（启动时你要填，默认 0.01）
  WAIT_FILL_TIMEOUT = 30     下单后等成交的超时秒数
  DEBUG_TRADE = True         ★ 是否把原始 API 请求/响应打到控制台

  ★ 特别说明 DEBUG_TRADE：打开时，控制台会打印每一笔的原始 OKX 响应。
    所以你会看到类似
        [DEBUG] 买入响应: {'code':'1','data':[{'sCode':'54000', ...}]}
    的一行——这是【被自动纠正的中间过程】，不是失败。
    v35 起程序会记住账户真正支持的 tdMode，只探测一次，之后不再出现这行。


【5】常见调参场景（照着改）
--------------------------------------------------------------------------------
▌场景 A：账户很小（几 U），总感觉钱没买进去
    → 看日志有没有 `IDLE x.xxU 闲置` 或 `SLOTS-THIN`。
    → 把小额场景设成：max_positions 4~5、max_position_ratio 0.25、thin_slot_guard 开
    → 原因：10 个仓要权益 ≥ 4U（4 × 0.985 ÷ 10 = 0.394U/仓 ≈ 交易所最小额）才吃得下。
      权益只有 1.6U 时 10 个仓 = 0.158U/仓，根本下不出去。

▌场景 B：总觉得「没在最高点卖」
    → 先确认日志里有 SELL 行（v27 及以前卖出不写日志，无法复盘）
    → 依次调：trail_tight_n 0.6 → 0.5（更贴近最高点，但更容易被扫出去）
              peak_lock_keep 0.65 → 0.75（保留更多涨幅，最坏回吐 25%）
              peak_lock_arm_atr 1.0 → 0.8（更早启动锁定）
    → 不要指望完美：最高点只能事后知道，目标是「把回吐从 6% 收到 2~3%」。

▌场景 C：弱市里老是买到妖币
    → 确认 breadth_gate = 1.2（不要设 1.0）
    → 想更严：设 1.4~1.5；想更宽松：1.1
    → 也可以把 min_vol_usdt 从 50 万提到 200 万（过滤掉小成交额币）
    → max_amp_pct 从 45 收到 30（更严格地剔除妖币）

▌场景 D：想拿得更久（不想被时间止损赶下车）
    → time_exit_days 1 → 3、max_hold_days 3 → 7
    → trend_reversal_t -2.0 → -3.0（对反转更迟钝）

▌场景 E：想更激进（多买多卖）
    → exposure_target 保持 0.985、max_positions 提到 15~20（记得 pick_n 同步）
    → min_cand_score 55 → 45（门槛降低，买入机会变多）
    → refresh_rounds 3 → 1（榜单更新更快）

▌场景 F：控制台太吵
    → 目前 DEBUG_TRADE 固定为开。若需要安静模式请反馈，可以做成开关。


【6】改了参数却没生效？排查清单
--------------------------------------------------------------------------------
================================================================================
